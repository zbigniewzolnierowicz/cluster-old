# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: nextcloud-secret
  namespace: default
stringData:
  username: ${BOOTSTRAP_NEXTCLOUD_USER_USERNAME}
  password: ${BOOTSTRAP_NEXTCLOUD_USER_PASSWORD}
  serverinfo_token: ${BOOTSTRAP_NEXTCLOUD_TOKEN}
  smtp_username: ${BOOTSTRAP_SMTP_USER}
  smtp_password: ${BOOTSTRAP_SMTP_PASSWORD}