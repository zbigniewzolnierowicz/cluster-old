# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
  namespace: flux-system
stringData:
  SECRET_DOMAIN: ${BOOTSTRAP_CLOUDFLARE_DOMAIN}
  SECRET_CLOUDFLARE_EMAIL: ${BOOTSTRAP_CLOUDFLARE_EMAIL}
  SECRET_SLACK_API_URL: ${BOOTSTRAP_SLACK_API_URL}
  SECRET_GRAFANA_PASSWORD: ${BOOTSTRAP_GRAFANA_PASSWORD}
  SECRET_TRANSMISSION_PASSWORD: ${BOOTSTRAP_TRANSMISSION_PASSWORD}
  SECRET_MINIO_ACCESS_KEY: ${BOOTSTRAP_MINIO_ACCESS_KEY}
  SECRET_MINIO_SECRET_KEY: ${BOOTSTRAP_MINIO_SECRET_KEY}
  SECRET_SMTP: ${BOOTSTRAP_SMTP}
  SECRET_SMTP_USER: ${BOOTSTRAP_SMTP_USER}
  SECRET_SMTP_PASSWORD: ${BOOTSTRAP_SMTP_PASSWORD}
  SECRET_AUTHELIA_REDIS_PASSWORD: ${BOOTSTRAP_AUTHELIA_REDIS_PASSWORD}
  SECRET_AUTHELIA_POSTGRES_PASSWORD: ${BOOTSTRAP_AUTHELIA_POSTGRES_PASSWORD}
  SECRET_AUTHELIA_USER_ZED_PASSWORD: ${BOOTSTRAP_AUTHELIA_USER_ZED_PASSWORD}
  SECRET_AUTHELIA_SECRET_KEY: ${BOOTSTRAP_AUTHELIA_SECRET_KEY}
  SECRET_AUTHELIA_STORAGE_ENCRYPTION_KEY: ${BOOTSTRAP_AUTHELIA_STORAGE_ENCRYPTION_KEY}