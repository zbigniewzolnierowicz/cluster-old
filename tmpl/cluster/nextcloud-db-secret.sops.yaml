# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: nextcloud-secret
  namespace: default
stringData:
  username: ${BOOTSTRAP_NEXTCLOUD_POSTGRES_USER}
  password: ${BOOTSTRAP_NEXTCLOUD_POSTGRES_PASSWORD}
  database: ${BOOTSTRAP_NEXTCLOUD_POSTGRES_DATABASE}